name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Generate make files with CMake
      run: |
        cd WireLib
        mkdir cmake
        cd cmake
    - name: run make
      run: make
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
